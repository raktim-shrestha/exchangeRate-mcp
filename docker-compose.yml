services:
  api-as-mcp:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - MCP_AUTH_TOKEN=${MCP_AUTH_TOKEN}
      - EXCHANGE_API_KEY=${EXCHANGE_API_KEY}
